# React Native / Metro does not reliably resolve pnpm's symlinked node_modules layout.
# Hoisting keeps Expo, Gradle and Xcode builds working across the workspace.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
