{
  "//": "The preview launcher cannot read files under ~/Desktop on this machine (EPERM on open). A probe running entirely from /tmp started fine, so the launcher itself works — the project's location is the constraint. These configurations therefore ATTACH to servers started from a context that does have access (`pnpm business`, `pnpm api`, `pnpm admin`), rather than spawning them. Move the repo to a path outside ~/Desktop with no spaces (e.g. ~/dev/try-sport) and the command-based configurations in the comment below will work directly — that move is also required for any native mobile build, since Metro and Gradle break on spaces in paths.",

  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "url": "http://localhost:3001",
      "port": 3001
    },
    {
      "name": "business",
      "url": "http://localhost:3001",
      "port": 3001
    },
    {
      "name": "api",
      "url": "http://localhost:3000",
      "port": 3000
    },
    {
      "name": "admin",
      "url": "http://localhost:3002",
      "port": 3002
    }
  ],

  "//onceRelocated": {
    "note": "After moving the repo off ~/Desktop, replace a configuration above with this shape to have the launcher start the server itself.",
    "example": {
      "name": "dev",
      "runtimeExecutable": "node_modules/.bin/next",
      "runtimeArgs": ["dev", "apps/business", "-p", "3001"],
      "port": 3001
    }
  }
}
